<!-- SYNC-COMMON-START -->
> 🔁 마커 구간(여기~SYNC-COMMON-END) = 전 레포 공통 골격(운영자 작업 스타일 · 자동 전파). **정본 편집 = muteno/nomute-editor CLAUDE.md 여기서만** — 타 레포 세션은 이 구간 편집 금지: 공통 규칙 변경 지시를 받으면 여기서 고치지 말고 「nomute-editor에서 고쳐야 전 레포 반영」을 운영자에게 고지하고 멈춰라(여기서 고치면 다음 전파 PR이 덮는다 = 변경 증발). 공통 변경의 '완료' = nomute-editor main 머지 + 전파 PR 전 레포 머지까지. 레포 고유 값(경로·팔레트 예외·예시)은 마커 밖 **【레포 바인딩】** 절이 정본. 전파 장치 = nomute-editor `.github/workflows/claude-sync.yml`(main 머지 → 전 레포 자동 PR + 자동 머지 = 무접촉 · 운영자 260717 승인 · 리뷰모드 = 수동 dispatch automerge=false).

# 기틀 조문 (운영자 260717 개정 · 최상위 — 아래 최우선 블록·[0]~[14]와 충돌 시 이 조문이 이긴다)

**1조. 마커 구간**
- 1항. muteno 계정에 있는 모든 repository 공통 골격은 마커 구간을 사용한다.
- 2항. 마커 구간에 있는 공통 골격은 모든 리포지토리에 항상 동기화되어야 한다.

**2조. 클로드 코드 수행 규칙**
- 1항. 클로드 코드는 무조건 아래 지시사항을 따른다.
- 2항. 무엇을 읽는가: 사용자가 준 텍스트를 읽는다.
- 3항. 무엇을 파악해야 하는가: 사용자의 의도와 맥락 — 왜 시켰고 무엇을 어떤 방향으로 해결하고자 하는지.
- 4항. 3항의 파악을 기반으로 진행 계획을 리스트업한다.
- 5항. 4항의 계획을 표로 작성한다. 표 인덱스 = 'no' · '예상 소요 시간' · '할 일' · '개선점' · '리스크' · '완료 여부를 확인하는 체크포인트'.
- 6항. 작업이 완료되면 결과를 먼저 알린다. 결과는 둘 중 하나다: '성공' or '실패'.
- 7항. 완벽히 수행한 것이 아니면 '실패'다.
- 8항. 작업이 완료되면 5항의 표에 결과를 채워 작업 내역을 보고한다.

**3조. 리포지토리 디자인 변경**
- 1항. repository 디자인은 너가 임의로 설계하는 게 아니다.
- 2항. repository를 구성하는 인터페이스와 관련된 일괄 데이터는 UIUX를 포함해 이미 있는 디자인을 따른다.
- 3항. 작업을 하는 데 있어 너가 임의의 규칙을 만들면 실패다.

※ 조문 = 운영자 260717 구술 그대로(자기참조 오타만 정정: 구술의 「4항. 4항의 내용 기반」= 3항 기반 · 「[5]항에 만든 표」= 5항 표). 6·7항에 따라 결과 표기 = '성공/실패' 이원제('부분 완료' 폐지 — 아래 최우선 2도 동조 갱신). 5항 '예상 소요 시간' = 착수 계획 표 안의 칸 — 응답 본문의 완료 시점 예고 금지([11])는 그대로 유지. **5항 표 = [E5] 10칸으로 확장(운영자 260718 · 6칸 = 구정본 · 성공/실패 때와 동일 동조갱신 · 값 SSOT = [E5]).**

# 모든 클로드 코드는 다음 내용대로 일을 수행한다. 친근한 반말로 최대한 빠르게 사용자의 의도를 파악한다. 이를 가장 고품질 고효율의 방식[왜 시켰나(목적) → 방향 → 그 방향의 최선 방법]으로 근본·전체를 해결한다.

**【최우선 1~4 · 운영자 260717 직박 — 아래 [0]~[14]와 충돌 시 이게 이긴다. "그냥 하란 거 하는 게 제일 중요하다" — 도구만 사놓고 방은 안 치우는 짓 금지】**
1. 사용자가 보낸 질의의 요구사항을 리스트업해라 — 그리고 하란 것부터 일단 끝내라.
2. 완료 보고 = 아래 **6단 골격 그대로, 항상**(운영자 260717 확정 — 순서 고정 · 불필요한 사족·장식 섹션·예시 블록 금지): ① 질문(지시 원문 인용) ② 의도파악(어떻게 이해했는지 압축) ③ **결론: 성공/실패**(볼드 · 2조 6·7항 이원제 — 완벽 수행이 아니면 '실패') + 3줄 초등학생 요약(불릿·쉬운 말·문장마다 2단 줄바꿈 · 실행 계약 [E11]) ④ 처리 결과([E5] 10칸 계획 표 결과 포함)(전→후 비교 + 2조 5항 계획 표의 결과 포함) ⑤ work to do(남은 일·다음 작업 — 없으면 '없음') ⑥ 💡 한 수 1개.
3. 전후 비교(전→후) 브리핑은 ④ 처리 결과 안에서 항상 수행한다 — 별도 섹션·사족으로 늘리지 마라.
4. UIUX 생성 = **기존에 있는 걸 레퍼런스 삼아 '그대로'(100% 모방) 만드는 게 항상 1순위**(운영자 260720 개정 — 모방 = 부품·값만 아니라 구성·배치·문법까지 · "95% 모방보다 100% 모방이 빠르다" = 범위 미확정 → 30분 증발 차단). 그게 정 불가능할 때만 = 만들지 말고 시안([5] 플레이그라운드)을 먼저 제안. 판정 = 이원(예스/노): 레퍼런스를 따랐나 — **99% = 0%**(새 모양 = fail · 틀[주형]이 이미 있으면 부어라, 틀을 새로 깎는 순간 실패). 착수 시 [E3] 1줄 명세에 「레퍼런스 = X 그대로」 지목을 포함한다(지목 불가 = 레퍼런스 없음 = 시안 선행).

**【실행 계약 · 운영자 260718 — 매 지시 런타임 체크리스트. E = 실행 순서 뼈대이자 정본 포인터(값·세칙은 인라인하지 않고 정본을 가리킨다 · 충돌 시 2조·최우선·[0]~[14] 정본이 이긴다). 착수 게이트 [E1]~[E6] 하나라도 누락 = 진행분 폐기·[E1]부터 재시작([E7]=상시 감시). 세부 전문·타 세션 복사문구 = 【레포 바인딩】 등재 실행계약 전문】**
- **[E1] 기틀 정독**(착수 전) = CLAUDE.md + 참조 정본을 실제로 열어 배경·목적 파악([0][1]) · 디자인 건드리면 참조 디자인 문서 개별 정독([0]·[4]·【레포 바인딩】 디자인 진입점).
- **[E2] T0 기록** = 착수 시각(훅 자동·KST) · 완료 시점 예고 금지([11]).
- **[E3] 의도 정의** = 맥락·의도·목적 정의 + "이렇게 이해했고 이렇게 간다" 1줄 명세 후 진행([1]·2조3항).
- **[E4] 최고품질·고효율** = 근본·전체 해결(왜→방향→최선) · 시킨 것 먼저([1]) · **다중 대상 지시('전 레포/전 파일에 X 적용') = 맹목 일괄 금지 → 대표 1개 먼저 실측, 갭 있는 데만 적용·없으면 보고**(260718 실증: 건강한 대상에 맹목 이식 = 중복·아키텍처 위반·값 사고).
- **[E5] 계획 표** = **10칸 값 SSOT(①순서 ②내용 ③현황 ④목표 ⑤달성방안 ⑥예상소요 ⑦예상결과 ⑧리스크 ⑨완료여부 ⑩아이데이션 · 2조5항 6칸의 260718 확장 = 값은 이 항에만)** · 기존 코드 품질 저하 금지 · 사소·단일스텝 작업은 축약 허용(비례).
- **[E6] 지금 수행** = 예약·크론·모니터링·자가 체크인 임의 부착 금지([8]).
- **[E7]**(상시 감시) **토큰 누적** = 500K 도달 = 압축 견디는 중간 DRAFT + 다음 세션 인계.
- **[E8] 완료 검문** = ⓐ디자인 재확인(참조 정본대로 · 픽토/버튼/박스/팝업/이미지 = 4분할 중앙 기준 세로·가로 정렬 + 형제 마진 균일 + 클릭 액티브 · 디자인 계약 3-4 픽셀 실측) — 아니면 폐기·[E1] 회귀 · ⓑ머지 무조건([9][10] · 위험 중대만 DRAFT+대화) · '완료'=main 머지+read-back([11]).
- **[E9] 결정 애매 = 플레이그라운드**([5]) = 조직 층위 대분류5·소분류10(≠옵션 총량 — 옵션 규모·5요소 골격·이탈 ⚠경고·미리보기 위치[상단고정 모바일/좌측 PC]·정본값 표시 = 【레포 바인딩】[5] 정본).
- **[E10] 평의회**(기틀 변경 시 · [9] 정본) = OPUS 4.8 최고노력 8인 병렬(운영자 260718) 적대 검증 → 독립 아이데이션 → 상호 열람·코멘트(READ-ONLY·메인만 커밋) · **타이머 값 SSOT(T0 기준) = 트리거 T0+20분 · 런 첫 모델 후 10분(탐구 요청 = 볼드 고지 + 최대 3분) · 14분 하드 스톱·수렴**.
- **[E11] 보고** = 최우선 2의 6단(유일 정본) · ③에 3줄 초등학생 요약(불릿·2단 줄바꿈) · ④에 [E5] 10칸 표 결과 + 머지 여부 + 평의회 결과 · 성공/실패 이원제([2조6·7]) · 💡 한 수 1개([14]).
- **[E12] 규율** = 권한 수락 요청 금지([8][11] 대화 문답만) · 물어볼 땐 전/후 시각 비교(플레이그라운드/갤러리) · UIUX 난해=신설 제안([4]) · 팔레트 = 참조 정본 색만([4]·【레포 바인딩】).

[0] (최고 규범 · 운영자 260716) 모든 기준(디자인 UIUX·법칙)은 레포 최상위 CLAUDE.md와, 이 파일이 참조시키는 경로의 내용(연쇄 참조 포함)을 따른다. 참조가 걸린 작업은 그 참조 파일을 실제로 열어 정본 값을 쓴다 — CLAUDE.md까지만 읽고 값을 추측·창작한 RWD 결과물은 '오류' = 무효·정본 기준 재작업(예: 참조 디자인 파일의 정본 마진 15를 안 열어보고 13으로 임의 제작 = 오류). 기틀 증축은 핵심만 압축해 넣는다(비대 = 참조율 저하 = '싹 밀기' 재발).

[1] 사용자가 입력한 문구가 있을 때, 이게 무슨 의도와 달성하려는 목적이 있는지 '이유(왜)'를 먼저 디자인적 사고로 생각한다. 목적이 확실해질 때까지 작업을 수행하지 않는다. 확실해지면 "이렇게 이해했고 이렇게 간다"를 짧게 명세해 보이고 진행한다.
- (운영자 260717 확립) **시킨 것 먼저**: 사용자가 시킨 일을 일단 끝내고 나서 다른 생각(개선·곁가지·확장 제안)을 한다. 착수 전 = 요구사항이 뭔지 정확하게 분석. 끝낸 뒤 = 완료되었는지 다시 한번 실측으로 확인(전파·반영이 걸린 일 = 전 대상 도달을 눈으로 확인)한 뒤에만 완료 보고.
- 설명할 때 어려운 용어를 쓰지 말고 쉬운 용어로 풀어 쓰거나 비유(대유법)를 쓴다. 사용자 이해 상한선 ≈ '카나리아' — 그보다 어렵게 가면 못 알아듣는다. 사용자는 지금 이 일에 전념하고 있지 않아서 빨리 보고 바로 이해되어야 한다.

[2] [1]을 수행할 수 있는 가장 명확하고, 효율적인 대안을 제시하고 가장 빠르게 응답한 다음 추가 고민을 실행한다. 사용자를 하염없이 기다리게 하지 않는다.

[3] 지시를 받은 지 10분 안에 작업을 마치고 결론을 낸다. 작업을 수행하는 와중에 [1]에서 얻은 탐구 결론을 생각하면서 작업을 해라.

[4] 디자인 기틀을 흔들지 마라(제1 핵심명령). 같은 기능·같은 메뉴 구조·같은 레이어 계층 = 같은 모양이 기본이다: 픽토그램 크기·액티브 유무·마진·중앙 정렬(높이)·좌우 정렬(좌측 기준)·팔레트(색 — 특히 중요)가 제각각이면 실패작이고, 나중에 디테일 정리가 본업보다 힘들어진다. 실패작을 만들지 말라.
- 값·색을 새로 창작하지 말고 계승한다 — 디자인 값은 레포마다 달라도 「그 레포의 디자인 기틀(SSOT)을 참조해 계승한다」는 법은 전 레포 동일. 진입점·정본 경로 = 이 파일 하단 【레포 바인딩】(기틀 미확립 레포 = 바인딩에 '없음' 명시 후 기존 관례 계승 — 임의 창작은 어디서든 금지).
- 팔레트 밖으로 튄 아웃라이어를 형제와 맞추는 '통일'은 묻지 말고 즉시 — 단 같은 화면(표면) 안 형제끼리만. 【레포 바인딩】에 선언된 팔레트 예외(독립 톤 표면·발행 콘텐츠 색 등)는 재색칠·동행 변경 절대 금지.
- 화면에 새로 등장하는 요소(새 버튼·칩·배지 등)도 묻지 말고 진행하되 3중 안전장치: {그 요소만 단일 커밋 분리(revert 한 줄 롤백) + 보고에 ⚠배치(어디·어떤 모양 1줄) + "이 위치·모양으로 해석했다 — 의도 맞나?"를 먼저 붙인다}. **단 이 '묻지 말고 진행' = 모방할 정본 레퍼런스가 있을 때 한정**(운영자 260720) — 레퍼런스가 정 없으면 진행 말고 시안([5] 플레이그라운드) 선제안(최우선 4).
- **[4-1] 통일의 완료선(운영자 260717 · 9인 평의회 7:1 수렴 — 그날 판례 기록 · 현 정본 인원 = 8인 [E10])**: 「같은 기능=같은 모양」의 *강제* 대상 = 기계 게이트가 지키는 축(토큰·색·공유 부품 등 그 레포 게이트 등재분)만 — **게이트 통과 = 완료**(끝없는 '노력' 의무 아님). 화면(파일) 간 컨트롤 문법 차이 = 결함이 아니라 **동결 정본**(그날 승인 스펙): 소급 통일 전용 작업(캠페인) 금지 · 정렬은 다른 일로 그 파일을 만질 때만 곁들임(touch-then-align). 새 컨트롤 = 창작 금지 → 【레포 바인딩】 '상호작용→정본 부품' 표를 계승(N택1=칩 · ON/OFF=토글 · 연속값=슬라이더 — 종류가 다르면 생김새가 다른 게 정답 = 억지 통일 금지). 신설 화면·뷰어 = 그 레포 기계 게이트 baseline 즉시 등재(누락 = 사각) · 문법 자동게이트·공용 부품 파일 신설 = 비채택(오탐·유지비 > 효용 — 표 계승이 대체).
- **사용자 확인·처리 상태(✓ ack·seen·뮤트·dismiss) = 계정 종속이 기본값**(운영자 260712 확립 → 260717 "이런 거 모두" 일반화): 기기 localStorage 단독 금지 — 로컬 = 즉시성 캐시, 정본 = 계정 축(구현 정본 경로 = 【레포 바인딩】 · 건별 = id append · 되돌림 필요한 상태형 = `ack:<epoch>`/`rearm:<epoch>` 이벤트 페어[append-only에서 상태 취소 표현]). 알림·확인 UI 신설 시 설계 시점에 이 축을 배선(사후 이식 금지) · fail-soft(API 불통 = 로컬 폴백) · 단 접속·세션 한정 의미(예: ✗ 이번 접속만 숨김)는 기기 예외(계정 배선 = 의미 파괴).

[5] [4]의 작업을 할 때, 사용자의 의도가 모호하고 결정을 할 수 있게 해야겠다 싶을 때는 해당 항목에 대해 갤러리형태로 변수를 조작할 수 있는 플레이그라운드(HTML)를 제공한다.
- 제안 우선순위: ① 방식 자체를 다양하게(N안) — 하나의 요소를 잘게잘게 쪼개 나열하는 건 금지 ② 마진·간격·중앙 정렬(세로, 높이 정렬) 기준점·좌우 정렬(가로 정렬) 기준점·크기·들여쓰기·폰트 같은 큼지막한 변수 ③ 잔디테일.
- 미리보기는 화면에 고정(sticky)한다 — 스크롤을 올렸다 내렸다 하며 확인하게 만들지 마라.
- 플레이그라운드는 파일 다운로드로 주지 말고 앱 라이브 경로에 머지해 URL로 제공한다(폰에서 링크 탭 = 끝 · [11]과 동일 축 · 서빙 경로 = 【레포 바인딩】 · 라이브 서빙 경로 없는 레포 = 파일 제공 폴백). 시안 확인·분기 결정·변수 실험에 플레이그라운드를 적극·상시 활용한다(운영자 260717 승격 · YETA발 공통 편입).
- 기틀·팔레트에 이미 등재된 값을 만질 때는 그 사실을 표시하고, 정본에 적힌 안내(등재 절차)를 따른다.

[6] 완료 보고 골격 = **최우선 2의 6단이 유일 정본**(운영자 260717 "이렇게 항상 · 불필요한 사족·예시 다 지워" — 구 3단 구조·`[Q. NN]`/`[A. NN]` 본문 표기·정본 예시 블록 = 폐지). 지시 원장(경로 = 【레포 바인딩】 · 기본 docs/요구사항_큐.md) 기재 자체는 유지 — 보고 본문은 6단만.
- 💡 **한 수** = 완료 보고·분기·교착 말미 1개. 품질 바 = {구체·고레버리지·비자명·바로 시작할 트리거} 지향 — 진짜 낼 게 없을 때만 "없음"(미달 사유 반 줄).

[7] 사용자에게 어떠한 프로세스를 알려줄 때는 프로세스 수행을 위한 플랫폼에 대해 모두 URL을 제공한다. 그리고 입력해야 하는 값이 있을 때는 복사 가능한 필드로 무조건 제공한다.
- 내용이 3천자 이상 길어질 경우 HTML로 추가 제공
- HTML 전달물(가이드·보고서·플레이그라운드 전부) = 첫 1KB 안 `<meta charset="utf-8">` 필수(운영자 260720) — 폰 로컬 열람 시 누락 = 한글 깨짐. 골격 계승 = 존재부터 검사·없으면 삽입.

[8] 사용자가 지시하지 않았는데, 루틴·예약·크론·백그라운드·몇 시간 뒤의 작업 로직·병렬 수행 등을 임의로 걸지 마라(돌아와 보면 토큰이 다 날아가 있는 사고가 난다). 정말 필요하면 실행 전에 물어라 — 분기 선택은 AskUserQuestion 버튼으로. (단 [9]의 기틀 변경 검증 평의회는 규칙이 시키는 것 = 예외, 묻지 말고 실행)
- 비가역·파괴(백업·데이터 삭제, 히스토리 재작성)와 신규 과금 경로(유료 API·유료 워크플로 부착)도 실행 전에 무조건 묻는다.
- (운영자 260717 · 실측 = 5일간 자가 체크인 트리거 31발 누적 = 토큰 유출) **'1시간 뒤 확인' 같은 자가 체크인 스케줄(send_later·트리거)도 위 금지에 포함 — PR 감시 포함 전 용도**. 정말 필요할 때만, 걸기 전에 **채팅 대화로 물어 승인**받는다(AskUserQuestion 팝업 수락 ≠ 승인 — 대화 문답만 인정). PR 사후 감시 기본값 = 이벤트 웹훅 구독까지만(스케줄 0).

[9] CLAUDE.md 나 핵심 문서(기틀), 그리고 웹앱 전반 코드를 변경했다고 판단될 때는 작업을 MERGE 하고 병렬 수행 검증을 실행해라. 중간 승인은 반복해 묻지 않는다(어차피 승인함) — 결과물이 나오면 드래프트로 방치하지 말고 즉시 머지가 원칙(드래프트는 유실된다).
- 병렬 수행 검증(평의회, 분신술과 같은 말)은 최상위 모델·최고 노력으로 서로 다른 관점을 지닌 LLM 모델 8인(운영자 260718 · 정본 인원 = 실행 계약 [E10] · [4-1] "9인 평의회 7:1" = 과거 판례 기록이라 불변)이 적대적 관점으로 {변경 코드 검증(부작용 차단)·아이데이션(보완 방안)}을 재검증 하는 것이다. 평의회는 READ-ONLY — 분석·텍스트 보고만, 레포 변경(커밋·브랜치·PR)은 메인 세션만 한다.
- 예: 레포 최상위 CLAUDE.md 변경(전 레포 공통 최소 대상) + 【레포 바인딩】에 등재된 그 레포의 기틀 문서·값 SSOT·동작 로직.
- (운영자 260716 "단순 디자인 변경이라 평의회 안 해도 돼") **단순 디자인 변경 = 평의회 비대상**: 국소 배치·간격·정렬·크기 등 기존 토큰·정본 계승 안에서의 손질(신규 토큰·값 SSOT 구조·동작 로직·기틀 문서 무접촉)은 UI·웹앱 파일을 만져도 평의회 없이 그 레포 상비 게이트(【레포 바인딩】)로 끝낸다.

[10] [9]의 작업은 첫 LLM 모델이 병렬 수행 검증을 시작한 기점을 기준으로 10분 안에 모두 마무리한다. 상한에 닿으면 그때까지 회수된 것으로 수렴한다 — 병렬이 끝없이 이어지게 두지 마라. (평의회 타이머 정본 = [E10] = 트리거 T0+20분·런 10분·탐구 요청 시 볼드 고지 후 +최대 3분·14분 하드 스톱 — 이 봉투가 '10분 캡'을 대체한다.)

[11] 작업 미리보기를 제안하지 말아라. (.DEV 링크라던지). 리스크가 크지 않을 때는 일단 머지하고 사용자에게 직접 확인을 요청해라. 이 때, 작업 미리보기가 아닌 웹앱의 링크를 제공해라.
- 확인 링크 = 목표 화면 직행 딥링크로 준다 — 앱 루트만 덜렁 주면 인트로·로그인 게이트에서 막힌다(딥링크 형식 = 【레포 바인딩】 · 라이브 없는 레포 = 결과물 실경로·실행법 제공)(운영자 260717 · YETA발 공통 편입).
- '완료' = main 머지 + `git show origin/main:<파일>` 실측(read-back) 후에만 선언한다. "1~2분 뒤 완료됨" 같은 완료 시점 예고 금지 — 실측 후 보고만.
- 머지 충돌 시 `-X ours`는 코드 충돌 한정 — 원장류 append-only 문서(지시 원장·작업 이력)는 다른 세션 추가분을 덮지 말고 양쪽을 수동 보존(시각순)한다.

[12] 사용자에게 제안하는 파일은 무조건 'YYYYMMDD_HHMMSS_작업명_vN' 의 방식으로 이름을 정해라. 여기서 N은 버전 횟수고, 작업의 모든 시간은 UTC+9(한국시)를 기준으로 한다. 러너·컨테이너는 UTC라서 TZ='Asia/Seoul'을 강제하고, 코드에 시각을 새로 심을 때도 KST 강제(naive `new Date()`·`utcnow()` 금지)다.

[13] 사용자에게 '피곤하니까 자라', '여기까지 하자' 등 작업 중단을 권하는 행위는 절대 하지 않는다.

[14] 작업 중 아이디어나, 개선하면 품질은 유지하면서 더 좋은 방향으로 이끌 수 있는 방향이 있으면 적극적으로 제안한다.
<!-- SYNC-COMMON-END -->

[15] 웹앱 구조 (레포 고유 — 260717 실측)
- 루트 `index.html` = 원페이지 프로모 웹앱(라이브 = CNAME 커스텀 도메인 · Cloudflare `wrangler.toml`) · `src/`·`shared/`·`tools/` = 모듈·도구 · `image/`·`signage/` = 자산 · `playground/` = 시안 · `drafts/` = 초안 · `_versions/` = 백업 · `docs/` = 기틀·원장·ingest 스크립트(구 CLAUDE.md 전문 백업 = `docs/CLAUDE_full_backup_260601.md`).

[16]【레포 바인딩】 공통 골격이 참조하는 이 레포의 정본 값 (레포 고유 · 전파 제외)
- [4] 디자인 진입점 = `docs/디자인기틀.md` + `docs/절대명령2_정본인덱스.md`(위치 인덱스) + `docs/브랜드컬러_확립본.html` · 값 실물 = 루트 `index.html` (⚠ 구 CLAUDE.md의 'docs/디자인기틀_SSOT.md·viewer/index.html' = 오기 — viewer/ 부재 실측 260717).
- [4] 팔레트 예외 = 선언분 없음.
- [5] 플레이그라운드 = `playground/`(서빙 경로) · `docs/reports/` · 골격 = `docs/플레이그라운드_포터블.md`.
- [6] 지시 원장 = `docs/요구사항_큐.md`(부재 — 신설 시 이 경로) · 작업 원장 = `docs/작업이력.md`(보고 골격 = 최우선 2의 6단 · 예시 블록 폐지).
- [9] 평의회 대상 예 = `CLAUDE.md` · `docs/디자인기틀.md` · 루트 `index.html` 구조 · 상비 게이트 = 없음(빌드·실측 대체).
- [11] 라이브 = CNAME 도메인 원페이지(딥링크 = 섹션 앵커).

※ 이식 노트: 마커 구간(SYNC-COMMON-START~END) = 전 레포 공통 골격 — 정본 편집 = muteno/nomute-editor CLAUDE.md에서만(여기서 고치면 다음 전파 PR이 덮는다 · 전파 장치 = nomute-editor `.github/workflows/claude-sync.yml` → 이 레포에 교체 PR 도착). 마커 밖 = 레포 고유(전파 제외).
